# Replit Agent 4 — Hyper-Detailed Per-Screen Prompts

## DESIGN SYSTEM (Include at top of EVERY prompt)

Every prompt below starts with this shared context:

```
DESIGN SYSTEM — apply to every element:
- Background: #000000 (pure black)
- Card/Surface: #18181B (zinc-900)
- Border: #27272A (zinc-800)
- Primary accent: #00D084 (green)
- Text primary: #FFFFFF
- Text secondary: #A1A1AA (gray-400)
- Text muted: #71717A (gray-500)
- Font: system-ui (system default)
- All headings: font-weight medium, tight letter-spacing
- All cards: rounded-lg, border zinc-800, bg zinc-900
- All buttons: rounded-lg, font-bold
- API base: https://rivo-partners-backend-331738587654.asia-southeast1.run.app/api/v1
- Auth: Bearer token in Authorization header, stored in AsyncStorage
```

---

## TASK 1: HomeScreen + Bottom Tab Navigation

```
DESIGN SYSTEM — [paste the design system block above]

Fix the HomeScreen AND bottom tab navigation to match the PWA exactly.

=== BOTTOM TAB BAR (affects all screens) ===

The tab bar must have 4 tabs with ICONS, not just text:
- Tab 1: Home icon (house/home outline), label "Home"
- Tab 2: Briefcase icon, label "Clients"  
- Tab 3: People/users icon, label "Network"
- Tab 4: Person/user icon, label "Profile"

Active tab: icon and label colored #00D084 (green)
Inactive tab: icon and label colored #71717A (gray)
Tab bar background: #000000 with top border #27272A
Tab icons should be from @expo/vector-icons Ionicons: "home-outline"/"home", "briefcase-outline"/"briefcase", "people-outline"/"people", "person-outline"/"person"

=== HOMESCREEN LAYOUT (top to bottom) ===

SECTION 1 — HEADER (sticky top, bg black, border-bottom zinc-800, padding horizontal 24, padding top 48, padding bottom 24):

Row 1: Left-aligned, horizontal layout:
  - Avatar circle: 48x48, background #27272A (zinc-800), rounded-full, centered white text showing FIRST CHARACTER of agent's name uppercased. If no name, show first char of phone. If nothing, show "?"
  - Right of avatar, vertical stack:
    - Agent's FIRST NAME only (split name by space, take first word). Font: 20px, medium weight, white. If no name, show full phone number.
    - Below name: Row with green dot (8x8, rounded-full, bg #00D084) + text "Online" (12px, gray-400, medium weight)

Row 2 (below header row, margin-top 32):
  - Label: "Total Paid" — 14px, gray-400, medium weight
  - Below: "AED {total_earned}" — 48px font, medium weight, white, tight tracking. total_earned comes from API field total_earned (parse as float, format with toLocaleString)

Row 3 (margin-top 24):
  - Two items side by side with 32px gap:
    - Item 1: Label "PENDING" (12px, gray-500, uppercase, tracking-wider) then "AED {pending_amount}" (18px, medium, white)
    - Item 2: Label "DISBURSALS" (12px, gray-500, uppercase, tracking-wider) then "{disbursed_count}" (18px, medium, white)

SECTION 2 — ACTIONS (padding 24):

Heading: "Actions" — 18px, medium weight, white, margin-bottom 16

Two cards side by side (grid, 2 columns on wider screens, stacked on narrow):

CARD 1 — "Submit Client":
  - Background: #18181B, rounded-lg, padding 16, full width
  - Layout: Row with items-center, space-between
  - Left side row:
    - Icon circle: 40x40, rounded-full, background WHITE (#FFFFFF), centered BLACK arrow icon rotated -45 degrees (pointing up-right). Use Ionicons "arrow-forward" or similar, rotated.
    - Right of icon, vertical:
      - "Submit Client" — 16px, medium, white
      - Row below: "Earn ~AED 9,000" (14px, gray-400) + green badge
      - Green badge: border 1px #00D084 with 50% opacity, bg black, rounded-full, padding horizontal 8 vertical 2, text "0.45% - 0.60%" in 10px bold #00D084 tracking-wide, with subtle green glow shadow
  - Right side: Chevron-right icon (20px, gray-600)
  - On tap: navigate to LeadSubmissionScreen

CARD 2 — "Invite Agent":
  - Same card style as above
  - Left side row:
    - Icon circle: 40x40, rounded-full, background #00D084 (green), centered BLACK users/people icon (Ionicons "people")
    - Right of icon, vertical:
      - "Invite Agent" — 16px, medium, white
      - "Get AED 2,000 Bonus" — 14px, gray-400
  - Right side: Chevron-right icon (20px, gray-600)
  - On tap: navigate to NetworkScreen

SECTION 3 — HOME BANNERS (only if banners exist):

Fetch banners from GET /api/v1/config/ — response has "home_banners" array.
Each banner object: {id, title, subtitle, icon, thumbnail, cta_text, cta_link, order}

For each banner, render a card:
  - bg zinc-900, border zinc-800, rounded-lg, overflow hidden
  - If thumbnail URL exists: show image at top, full width, 160px height, object-cover
  - Below image (or at top if no image): padding 16
    - Row: emoji icon (if exists, 24px) + vertical stack:
      - title: 16px, medium, white
      - subtitle: 14px, gray-400, margin-top 4
      - If cta_text and cta_link exist: green text link "cta_text >" (14px, medium, #00D084)
        - If cta_link starts with "/": navigate internally
        - Otherwise: open external URL

SECTION 4 — COMPLETE PROFILE NUDGE (only if user.is_profile_complete is false):

Card: bg zinc-900/50, border zinc-800, rounded-lg, padding 16
  - Row: space-between
  - Left: "Complete your profile" (14px, medium, white) + "Add your name, type, and email." (12px, gray-400)
  - Right: Chevron-right (20px, gray-600)
  - On tap: navigate to ProfileScreen

=== API CALLS ===
- On screen mount: GET /agents/me/ — returns {name, phone, total_earned, pending_amount, disbursed_count, this_month_earned, is_profile_complete, agent_code, ...}
- On screen mount: GET /config/ — returns {commission_min_percent, commission_max_percent, avg_payout, referrer_bonuses, new_agent_bonuses, home_banners, ...}
- Store config values: avg_payout (default 9000), commission_min_percent (default 0.45), commission_max_percent (default 0.60), referrer_bonuses (default [500,500,1000])
- TOTAL_POTENTIAL = sum of referrer_bonuses array (500+500+1000 = 2000)

=== CRITICAL: ICONS ===
Every icon MUST render. Use @expo/vector-icons with Ionicons. Test that every icon actually shows. If any icon renders as blank/invisible, the task is NOT complete.
```

---

## TASK 2: ClientsScreen

```
DESIGN SYSTEM — [paste the design system block above]

Fix the ClientsScreen to match the PWA exactly.

=== LAYOUT (top to bottom) ===

HEADER (sticky top, bg black, padding horizontal 24, padding top 48, padding bottom 16, border-bottom zinc-800):

Row: space-between, items-center, margin-bottom 24:
  - Left: "Clients" — 30px, medium weight, white, tight tracking
  - Right: "Add Client" button — background white, text black, 14px, font-semibold, padding horizontal 16 vertical 8, rounded-lg. On tap: navigate to LeadSubmissionScreen

Below the header row:
  - Search input: full width, height 48, padding-left 40, rounded-lg, bg #18181B, no border, text white, placeholder "Search clients..." in zinc-600
  - Search icon (Ionicons "search") positioned absolute, left 12, vertically centered, 20px, gray-500
  - On text change: debounce 300ms, then call API with search query

=== CLIENT LIST ===

API: GET /clients/?search={searchQuery}
Response is array of: {id, client_name, client_phone, expected_mortgage_amount, estimated_commission, commission_amount, status, created_at, updated_at}

If loading: show centered spinner (24px, white, spinning)
If empty array: show centered "No clients yet." (gray-500) + "Submit your first client to start earning" (gray-500, smaller)

For each client, render a card (NOT a bordered card — use border-bottom zinc-800, padding-bottom 16, last item no border):

Row 1: space-between, items-start:
  - Left vertical:
    - client_name: 18px, medium, white
    - Date: 12px, gray-500, margin-top 4. Format created_at as "7 Mar 2026" using toLocaleDateString with month:"short", day:"numeric"
  - Right: Status badge
    - Text: Use STATUS_LABELS mapping:
      SUBMITTED → "Submitted"
      CONTACTED → "Contacted"
      QUALIFIED → "Qualified"
      SUBMITTED_TO_BANK → "Submitted to Bank"
      PREAPPROVED → "Preapproved"
      FOL_RECEIVED → "FOL Received"
      DISBURSED → "Disbursed"
      DECLINED → "Declined"
    - Color: DISBURSED = #00D084 (green), DECLINED = #71717A (zinc-500), ALL OTHERS = #A1A1AA (gray-400)
    - Font: 12px, medium, tracking-wide

Row 2 (margin-top 8): space-between, items-end:
  - Left vertical:
    - "Loan Amount" — 12px, gray-500
    - "AED {expected_mortgage_amount}" — 16px, medium, gray-300. Format with toLocaleString
  - Right vertical (text-align right):
    - "Est. Commission" — 12px, gray-500
    - "AED {commission_amount || estimated_commission}" — 18px, medium, white. Use commission_amount if not null, else estimated_commission. Format with toLocaleString

Bottom padding: 96px (to clear tab bar)
```

---

## TASK 3: NetworkScreen

```
DESIGN SYSTEM — [paste the design system block above]

Fix the NetworkScreen to match the PWA exactly.

=== API CALLS ===
- GET /agents/network/ — returns:
  {
    agent_code: "RIVO-WVSJ",
    referred_agents: [{id, name, agent_code, created_at, deals_count, bonus_earned}],
    bonus_summary: {total_earned, bonuses_count, max_bonuses, completed, bonuses: []}
  }
- GET /config/ — returns {referrer_bonuses: [500,500,1000], referral_share_msg: "Hey, I'm using Rivo to earn mortgage commissions. Join here: "}

=== LAYOUT (top to bottom) ===

HEADER (bg black, padding top 48, padding bottom 32, padding horizontal 24, border-bottom zinc-800):

Centered content:
  - "My network" — 30px, medium, white, tight tracking, centered, margin-bottom 24
  - Label: "Your Referral Code" — 14px, gray-400, centered, margin-bottom 16
  - Code box: bg #18181B, border zinc-800, rounded-lg, padding 16, centered
    - Agent code text: "RIVO-WVSJ" (from API agent_code) — 36px, medium, white, tracking-widest, centered. MUST be real code from API, NOT placeholder "RIVO-XXXX"
  
  - "Invite your network" button: full width, height 56, bg white, text black, 18px, medium, rounded-lg, margin-top 24
    - Left: share icon (Ionicons "share-social-outline", 20px)
    - On tap: open native share sheet with text: "{referral_share_msg}https://partners.rivo.ae?ref={agent_code}"

  - "Tap to copy link" — centered, 14px, gray-400, margin-top 16
    - Left: copy icon (Ionicons "copy-outline", 16px, gray-400)
    - On tap: copy "https://partners.rivo.ae?ref={agent_code}" to clipboard, show brief toast/feedback

  - Divider line (zinc-800/50, margin-top 24, padding-top 16)
  - "Know more about the referral program >" — centered, 14px, medium, #00D084, with chevron-right icon
    - On tap: navigate to ReferralInfoScreen

=== HOW IT WORKS SECTION (padding 24) ===

Heading: "How it works" — 18px, medium, white, margin-bottom 24

3 steps, each with vertical layout and connecting line:

Step 1:
  - Circle: 32x32, rounded-full, bg zinc-900, border zinc-800, centered white text "1" (14px, medium)
  - Connecting line below circle (2px wide, zinc-800)
  - Right of circle: "Ask Agents to join Rivo" (16px, medium, white) + "Share your link with agents in your network." (14px, gray-400, margin-top 4)

Step 2:
  - Same circle style with "2"
  - "They refer mortgages to Rivo" + "Bonuses unlock on their first 3 disbursals." (use referrer_bonuses.length instead of hardcoded 3)

Step 3:
  - Circle: bg #00D084, text black "3"
  - "You both get paid" + "AED 500, AED 500, AED 1,000 respectively." (format from referrer_bonuses array)

Terms note (margin-top 32, border-top zinc-900, padding-top 24):
  - "*Terms and conditions apply..." — 12px, gray-500
  - Link: "Read full T&Cs" — gray-400, underline

=== YOUR NETWORK SECTION ===

Heading row: "Your Network" (18px, medium, white) + people icon (Ionicons "people", 20px, white, margin-right 8)
  - If bonus_summary.completed: show green badge "Completed" (bg green/10, text green, 12px, rounded-full)

If loading: show 2 skeleton placeholder rows
If referred_agents is empty: "No agents in your network yet. Share your link to start earning bonuses." (14px, gray-500)

For each referred agent:
  - Row: space-between, border-bottom zinc-900, padding vertical 8
  - If deals_count is 0: reduce opacity to 50%
  - Left:
    - Name: 16px, medium, white (or "Agent" if no name)
    - "Joined {date} • {deals_count}/{max_bonuses} Deals" — 12px, gray-500
  - Right:
    - "+AED {bonus_earned}" — 14px, medium. Color: green if bonus_earned > 0, else gray-500
    - "Bonus" — 10px, gray-500, uppercase

Bottom padding: 96px
```

---

## TASK 4: ProfileScreen

```
DESIGN SYSTEM — [paste the design system block above]

Fix the ProfileScreen to match the PWA exactly.

=== API CALLS ===
- GET /agents/me/ — returns full agent profile
- PATCH /agents/profile/ — update fields {name, email, agent_type, agent_type_other, rera_number}
- POST /agents/logout/ — clear session
- DELETE /agents/delete/ — soft delete account
- POST /agents/connect-google/ — with {credential}
- POST /agents/connect-outlook/ — with {code, redirect_uri}

=== STATE ===
- isEditing: boolean — starts as true if is_profile_complete is false, else false
- name, email, agentType, agentTypeOther, reraNumber: from API
- saving: boolean loading state
- showDeleteConfirm: boolean

=== LAYOUT (top to bottom, scrollable) ===

HEADER (bg black, padding horizontal 24, padding top 48, padding bottom 24, margin-bottom 24, border-bottom zinc-800):

Row: space-between:
  - Left: "Profile" — 30px, medium, white
  - Right: "Edit" button (only when NOT editing) — green text, 14px, medium. On tap: setIsEditing(true)

Below heading:
  - Row: items-center, gap 16
  - Avatar: 80x80, rounded-full, bg #18181B (zinc-900), centered
    - Show person icon (Ionicons "person-outline", 40px, gray-400) — NOT just a dark circle
  - Right of avatar, vertical:
    - Name: 20px, medium, white (or "Set your name" if empty)
    - Phone: 16px, gray-400
    - Badge: "Verified Partner" — margin-top 8, padding horizontal 12 vertical 4, bg green/10, text #00D084, 12px, medium, rounded-full

=== PERSONAL DETAILS SECTION (padding horizontal 24) ===

Section heading: "Personal Details" — 18px, medium, white, margin-bottom 24

Fields (each with 16px gap):

1. Full Name input:
   - Label: "Full Name" — 14px, gray-400, margin-bottom 8
   - Input: height 56, bg zinc-900, border zinc-800, rounded-lg, padding horizontal 16, text white, 16px
   - If NOT editing: opacity 70%, read-only

2. Agent Type dropdown:
   - Label: "Agent Type" — 14px, gray-400
   - Picker/Select: same input style, with chevron-down icon on right
   - Options: "Select type" (placeholder), "Real Estate Broker" (value RE_BROKER), "Mortgage Broker" (value MORTGAGE_BROKER), "Other" (value OTHER)
   - MUST show current value from API, not always "Select type"
   - If NOT editing: disabled, opacity 70%

3. CONDITIONAL — Agent Type Other (only when agentType === "OTHER"):
   - Label: "What do you do?"
   - Same input style

4. CONDITIONAL — RERA Number (only when agentType === "RE_BROKER"):
   - Label: "RERA Number"
   - Placeholder: "BRN-XXXXX"
   - Same input style

=== CONNECT ACCOUNTS SECTION ===

Section heading: "Connect Accounts" — 18px, medium, white, margin-bottom 24

If editing AND no email set yet, show:
  - Google button: full width, height 48, bg zinc-900, border zinc-800, rounded-lg. Row: Google "G" logo (colorful SVG) + "Continue with Google" (14px, medium, white)
  - Outlook button: same style. Row: Microsoft logo + "Continue with Outlook"
  - Divider: centered "or" text in gray-500

5. Email input:
   - Label: "Email Address"
   - Placeholder: "name@example.com"
   - Same input style

If editing:
  - "Save Profile" button: full width, height 48, bg white, text black, medium. On tap: PATCH /agents/profile/ with {name, email, agent_type, agent_type_other, rera_number}. On success: refreshUser, setIsEditing(false), show toast.
Else:
  - "Edit Profile" button: full width, height 48, bg black, border zinc-700, rounded-lg. Row: pencil icon + "Edit Profile" (14px, medium, white). On tap: setIsEditing(true)

=== NOTIFICATIONS SECTION ===

Section heading: "Notifications" — 18px, medium, white

Push Notifications toggle row:
  - Row: space-between, padding 16, bg zinc-900, border zinc-800, rounded-xl
  - Left: bell icon (Ionicons "notifications-outline") + "Push Notifications" (14px, medium, white)
  - Right: toggle switch (green when ON, zinc-700 when OFF, white knob)

=== SIGN OUT + DELETE (padding-top 24, border-top zinc-800) ===

Sign Out button: full width, bg zinc-900, border zinc-800, text RED (#EF4444), rounded-lg, height 48
  - Row: logout icon (Ionicons "log-out-outline", red) + "Sign Out"
  - On tap: POST /agents/logout/ → clear AsyncStorage → navigate to Landing

Delete Account: full width, text gray-500, 14px
  - Row: trash icon + "Delete Account"
  - On tap: show confirmation
  - Confirmation: bg red/10, border red/20, rounded-lg, padding 16
    - "Are you sure? This cannot be undone." (14px, red-400, centered)
    - Two buttons: "Cancel" (gray, border zinc-800) + "Delete" (red, border red/30, bg red/10)
    - On delete confirm: DELETE /agents/delete/ → clear all AsyncStorage → navigate to Landing

Bottom padding: 96px
```

---

## TASK 5: LandingScreen + WhatsAppListeningScreen + LeadSubmissionScreen + ReferralSuccessScreen

```
DESIGN SYSTEM — [paste the design system block above]

Fix these 4 screens to match the PWA exactly.

=== LANDING SCREEN ===

Full screen, bg black, padding 24.

TOP (margin-top 32, margin-bottom 48):
  - Row: space-between
  - Left row: Green square (32x32, bg #00D084, rounded-lg, centered black "R" text 18px medium) + "Rivo Partners" (20px, medium, white, tight tracking)
  - Right (only if referral code in URL): referral badge — bg zinc-900, border zinc-800, rounded-lg, padding horizontal 16 vertical 8. Row: pulsing green dot (8px) + vertical: "REFERRED BY" (10px, gray-400, uppercase, tracking-wider) + agent name (14px, medium, white)

CENTER (flex-1, centered vertically):
  - Heading: "Earn on every" (line 1) + "mortgage you refer." (line 2, colored #00D084) — 48px, medium, tight tracking, leading 1.1
  
  - Three value props (margin-top 48, gap 32 between each):
    - Row 1: Zap/lightning icon (Ionicons "flash", 24px, #00D084) + "Fast Referrals" (18px, medium, white) + "Submit a client in 60 seconds." (16px, gray-400)
    - Row 2: Activity/chart icon (Ionicons "trending-up", 24px, #00D084) + "Real-Time Tracking" + "Live updates on every case."
    - Row 3: Users/people icon (Ionicons "people", 24px, #00D084) + "Network Bonus" + "Earn by referring other agents."

  - Payout section (margin-top 48, border-top zinc-800, border-bottom zinc-800, padding vertical 24):
    - Row: "AED 9,000" (36px, medium, white) with superscript "^" + green badge "0.45% - 0.60%" (same green badge style as HomeScreen)
    - Below: "Average payout per deal · on a AED 1.8M loan" (14px, gray-500, medium)

BOTTOM (margin-top auto, padding-top 32):
  - Terms checkbox row: checkbox (20px, green accent when checked) + "I agree to the Terms & Conditions" (14px, gray-400) with "Terms & Conditions" as green underlined link
  
  - "Get Started" button: full width, height 56, bg white, text black, 18px, bold, rounded-lg
    - WhatsApp icon SVG (28px, colored #25D366) on left
    - DISABLED (opacity 50%) when terms not checked
    - On tap: call POST /agents/init-whatsapp/ with {referral_code: stored_code, is_whatsapp_business: false, is_sign_in: false} → store code → open WhatsApp URL via Linking → navigate to WhatsAppListeningScreen

  - "Already have an account? Sign In" (14px, gray-400, "Sign In" underlined white)
    - DISABLED when terms not checked
    - On tap: same as Get Started but with is_sign_in: true

  - WhatsApp picker bottom sheet (when user has no saved WhatsApp preference):
    - Overlay: black/80, blur
    - Sheet from bottom: bg zinc-900, rounded-top 24px, padding 32
    - Handle bar: 48x4, zinc-700, rounded, centered
    - "Continue with WhatsApp" (20px, medium, white) + "Select your preferred account." (14px, gray-400)
    - Two buttons grid (2 columns):
      - "Personal": WhatsApp green icon (64px circle) + "Personal" label
      - "Business": WhatsApp Business icon (64px circle) + "Business" label
    - "Cancel" at bottom (gray-500)

=== WHATSAPP LISTENING SCREEN ===

Full screen, bg black, padding 24.

Back button: top-left, padding 8, rounded-full, Ionicons "arrow-back" (24px, white). On tap: go back.

Center (flex-1, centered):
  - Pulsing WhatsApp circle: 96x96, bg #25D366, rounded-full, centered WhatsApp SVG icon (48px, white)
  - Behind it: same circle with opacity 20%, animate-ping effect (pulsing outward)
  - Shadow: green glow [0_0_30px_rgba(37,211,102,0.3)]
  
  - "Tap Send in WhatsApp" — 30px, medium, white, margin-top 32
  - "A message with your code is ready — just hit send." — 18px, gray-400, margin-top 16, centered, max-width 300
  
  - Listening indicator: bg zinc-900/50, padding horizontal 24 vertical 12, rounded-full, border zinc-800
    - Row: pulsing green dot (8px, animate-pulse) + "Listening..." (14px, gray-300, medium)
  
  - After 6+ seconds: "Taking longer than expected? Try opening WhatsApp again." (14px, gray-500)

Bottom: "Open WhatsApp again" — green text, 14px, medium. On tap: re-open WhatsApp URL.

POLLING LOGIC:
  - Every 2 seconds: GET /agents/check-verification/{code}/
  - If response.verified === true:
    - Store response.token in AsyncStorage
    - Store response.agent in AsyncStorage
    - If agent.has_completed_first_action is false → navigate to ReferralBonusScreen
    - Else → navigate to Home tab

=== LEAD SUBMISSION SCREEN ===

Full screen, bg black.

HEADER (sticky, bg black, padding horizontal 24, padding top 24, padding bottom 16, border-bottom zinc-800):
  - Row: back button (Ionicons "arrow-back", 24px, white, padding 8) + "New Client" (20px, medium, white)

  - Commission calculator card (bg zinc-900, rounded-lg, border zinc-800, padding 16, margin-top 16):
    - Row: space-between
    - Left: "Estimated Commission" (14px, gray-400, medium) + "AED {calculated}" (24px, medium, #00D084)
    - Right: "0.45% of loan" (12px, gray-500)
    - Calculation: mortgage_amount × 0.45 / 100 — updates in real-time as user types amount

FORM (padding 24):
  Section heading: "Client Details" — 18px, medium, white, margin-bottom 24

  Field 1 — Full Name:
    - Label: "Full Name" (14px, gray-400)
    - Input: height 56, bg zinc-900, border zinc-800, rounded-lg, text white, placeholder "e.g. Ahmed Al-Mansoor"

  Field 2 — Expected Loan Amount:
    - Label: "Expected Loan Amount (AED)"
    - Input: numeric keyboard, placeholder "e.g. 1500000"
    - Strip non-numeric characters on input
    - On change AND on blur: recalculate commission

  Field 3 — Phone Number:
    - Label: "Phone Number"
    - Row: country picker + phone input
    - Country picker: height 56, bg zinc-900, border zinc-800, rounded-lg, shows flag emoji + country code
    - 11 countries with EXACT digit validation:
      {code:"+971", flag:"🇦🇪", label:"UAE", digits:9}
      {code:"+91", flag:"🇮🇳", label:"IN", digits:10}
      {code:"+44", flag:"🇬🇧", label:"UK", digits:10}
      {code:"+1", flag:"🇺🇸", label:"US", digits:10}
      {code:"+966", flag:"🇸🇦", label:"SA", digits:9}
      {code:"+968", flag:"🇴🇲", label:"OM", digits:8}
      {code:"+974", flag:"🇶🇦", label:"QA", digits:8}
      {code:"+973", flag:"🇧🇭", label:"BH", digits:8}
      {code:"+965", flag:"🇰🇼", label:"KW", digits:8}
      {code:"+92", flag:"🇵🇰", label:"PK", digits:10}
      {code:"+63", flag:"🇵🇭", label:"PH", digits:10}
    - Phone input: keyboardType phone-pad, strip non-digits on change
    - Validation on blur: "Enter {digits} digits for {label}" if wrong length
    - Default country: +971 (UAE)

  Consent section: bg zinc-900, rounded-lg, border zinc-800, padding 16
    - Row: shield-check icon (Ionicons "shield-checkmark", 20px, gray-400) + text
    - "We'll contact them within 30 minutes. By submitting, you confirm you have the client's consent to be contacted by Rivo." (12px, gray-400)

  Error display (if any): bg red/10, border red/20, rounded-lg, padding 12
    - Error text in red-400, 14px

  Submit button: full width, height 56, bg white, text black, 18px, medium, rounded-lg
    - "Submit Client"
    - DISABLED when: name empty OR phone empty OR amount empty OR phone validation error
    - Show loading spinner when submitting
    - API: POST /clients/ingest/ with {client_name, client_phone: "{countryCode}{phone}", expected_mortgage_amount: parseFloat(amount), consent: true}
    - Error "This client has already been referred" → show in error display
    - Success → navigate to ReferralSuccessScreen

=== REFERRAL SUCCESS SCREEN ===

Full screen, bg black, centered content.

Animated checkmark: 96x96 circle, bg #00D084, centered
  - White checkmark icon inside (Ionicons "checkmark", 40px, black, strokeWidth 4)
  - Animate: scale from 0.5 to 1, opacity 0 to 1, spring animation
  - Green glow shadow

"Client Submitted" — 30px, medium, white, margin-top 32
"We've received your client details." — 18px, gray-400, margin-top 16

Two info cards (margin-top 48):
  Card 1: bg zinc-900, rounded-lg, border zinc-800, padding 16, row
    - Icon: 40x40, rounded-full, bg zinc-800, centered message icon (Ionicons "chatbubble", 20px, white)
    - "Updates via WhatsApp" (16px, medium, white) + "You'll receive status changes instantly." (14px, gray-500)
  
  Card 2: same style
    - Icon: 40x40, rounded-full, bg zinc-800, centered briefcase icon (Ionicons "briefcase", 20px, #00D084, pulsing)
    - "Track in Clients" + "View progress in your dashboard."

Bottom: "Done" button — full width, height 56, bg white, text black, 18px, medium, rounded-lg
  - On tap: navigate to Home tab

=== CRITICAL ===
Every single icon MUST render as a visible icon. Use @expo/vector-icons Ionicons for ALL icons. If any icon appears as a blank square, dark circle, or invisible element, the task is FAILED.
```

---

## HOW TO USE THESE PROMPTS

1. Wait for current Agent task to finish
2. Click "+ New task" for Task 1 — paste the HomeScreen prompt
3. Click "+ New task" for Task 2 — paste the ClientsScreen prompt
4. Click "+ New task" for Task 3 — paste the NetworkScreen prompt
5. Click "+ New task" for Task 4 — paste the ProfileScreen prompt
6. Click "+ New task" for Task 5 — paste the Landing+Submission prompt
7. All 5 run in parallel
8. Test each screen on phone
9. Report any remaining issues to me