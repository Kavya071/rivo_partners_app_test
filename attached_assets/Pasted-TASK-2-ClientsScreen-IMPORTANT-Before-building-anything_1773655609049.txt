TASK 2: ClientsScreen

IMPORTANT: Before building anything, first READ the actual PWA source code from the cloned repo "Rivo partners GitHub repository". Read each file LINE BY LINE. The PWA is the source of truth — match it exactly.
READ THESE FILES FIRST:

Rivo partners GitHub repository/frontend/src/screens/ClientsScreen.tsx
Rivo partners GitHub repository/frontend/src/lib/api.ts

DESIGN SYSTEM:

Background: #000000, Card: #18181B, Border: #27272A, Accent: #00D084
Text primary: #FFFFFF, Text secondary: #A1A1AA, Text muted: #71717A
ALL icons: @expo/vector-icons Ionicons — NO web-only icons
API base: https://rivo-partners-backend-331738587654.asia-southeast1.run.app/api/v1
Auth: Bearer token from AsyncStorage

Fix the ClientsScreen to match the PWA exactly.
=== HEADER (sticky, bg #000000, paddingHorizontal 24, paddingTop 48, paddingBottom 16, borderBottom #27272A) ===
Row (justifyContent space-between, alignItems center, marginBottom 24):

Left: Text "Clients" — fontSize 30, fontWeight 500, color white, letterSpacing -0.5
Right: Pressable button — backgroundColor white, paddingHorizontal 16, paddingVertical 8, borderRadius 8. Text "Add Client" fontSize 14, fontWeight 600, color black. On press: navigate to LeadSubmissionScreen

Search input (below header row):

Container: width 100%, height 48, borderRadius 8, backgroundColor #18181B, flexDirection row, alignItems center, paddingLeft 12
Left: Ionicons name="search" size={20} color="#71717A"
TextInput: flex 1, marginLeft 8, color white, fontSize 16, placeholder "Search clients..." placeholderTextColor="#52525B"
On text change: debounce 300ms, then re-fetch clients with search query

=== CLIENT LIST ===
API: GET /clients/?search={searchQuery}
Response: array of {id, client_name, client_phone, expected_mortgage_amount, estimated_commission, commission_amount, status, created_at, updated_at}
If loading: centered ActivityIndicator size 24, color white
If empty: centered text "No clients yet." (color #71717A) + "Submit your first client to start earning" (color #71717A, fontSize 14)
FlatList of clients. Each item (borderBottomWidth 1, borderBottomColor #27272A, paddingBottom 16, marginBottom 16, last item no border):
Row 1 (flexDirection row, justifyContent space-between, alignItems flex-start):

Left:

client_name: fontSize 18, fontWeight 500, color white
Date (marginTop 4): fontSize 12, color #71717A. Format created_at: new Date(created_at).toLocaleDateString("en-AE", {month:"short", day:"numeric"}) → e.g. "7 Mar"


Right: Status text

Labels mapping:
SUBMITTED → "Submitted"
CONTACTED → "Contacted"
QUALIFIED → "Qualified"
SUBMITTED_TO_BANK → "Submitted to Bank"
PREAPPROVED → "Preapproved"
FOL_RECEIVED → "FOL Received"
DISBURSED → "Disbursed"
DECLINED → "Declined"
Colors: DISBURSED = #00D084, DECLINED = #71717A, ALL OTHERS = #A1A1AA
Style: fontSize 12, fontWeight 500, letterSpacing 0.5



Row 2 (marginTop 8, flexDirection row, justifyContent space-between, alignItems flex-end):

Left:

"Loan Amount" (fontSize 12, color #71717A)
"AED {expected_mortgage_amount}" (fontSize 16, fontWeight 500, color #D4D4D8). Format: parseFloat then toLocaleString


Right (alignItems flex-end):

"Est. Commission" (fontSize 12, color #71717A)
"AED {commission_amount || estimated_commission}" (fontSize 18, fontWeight 500, color white). Use commission_amount if not null, else estimated_commission. Format with toLocaleString.



Bottom padding: 96 (to clear tab bar)